import React from "react";
import styles from "./flow-chart.module.css";

const FlowChart = () => {
  return <div className={`flex-c ${styles.flowChart}`}>FlowChart</div>;
};

export default FlowChart;
